# TIL — Navigation Patterns

Navigation should be predictable and consistent across screens.
Common patterns: top bar, side drawer, bottom tab bar.
Users should always know where they are and where they can go.
